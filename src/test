# Raylib_VSCode_Mac_Template


to run code : 

Mac :
```
  clang -framework CoreVideo -framework IOKit -framework Cocoa -framework GLUT -framework OpenGL libraylib.a main.c -o my_app
```

Windows :

``` 
gcc -framework CoreVideo -framework IOKit -framework Cocoa -framework GLUT -framework OpenGL libraylib.a main.c -o my_app
```
